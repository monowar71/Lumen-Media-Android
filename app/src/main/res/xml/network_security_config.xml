<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev servers often run on LAN / emulator loopback without TLS. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
